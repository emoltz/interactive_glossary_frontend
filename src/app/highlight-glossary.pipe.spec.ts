import { HighlightGlossaryPipe } from './highlight-glossary.pipe';

describe('HighlightGlossaryPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightGlossaryPipe();
    expect(pipe).toBeTruthy();
  });
});
